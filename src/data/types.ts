enum Type {
  AUTO = 'auto',
  UNKNOWN = 'unknown',
  TEXT = 'text',
  NUMBER = 'number',
  DATE = 'date',
  RELATIVE_DATE = 'relative-date',
  DATETIME = 'datetime',
  RELATIVE_DATETIME = 'relative-datetime',
  TIME = 'time',
  RELATIVE_TIME = 'relative-time',
  BOOLEAN = 'boolean',
}

export default Type;
