import colors from './_colors.scss';

enum Color {
  BLUE = colors.BLUE,
  NIGHT_BLUE = colors.NIGHT_BLUE,
  RED = colors.RED,
  GREEN = colors.GREEN,
  NIGHT_GREEN = colors.NIGHT_GREEN,
  BEIGE = colors.BEIGE,
  DARK_BEIGE = colors.DARK_BEIGE,
  LIGHT_GREY = colors.LIGHT_GREY,
}

export default Color;
